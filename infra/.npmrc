# Auth for the local verdaccio registry. The registry allows anonymous publish
# for @tuler/* (see verdaccio/config.yaml); npm still wants a token present, so
# this placeholder satisfies the CLI. Used via `npm publish --userconfig`.
//localhost:4873/:_authToken="local-anonymous"
@tuler:registry=http://localhost:4873/
